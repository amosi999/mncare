import 'package:flutter/material.dart';

class PetDoctorScreen extends StatelessWidget {
  const PetDoctorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('펫닥터 화면'),
    );
  }
}
